<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>TidyUp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://code.jquery.com/jquery-2.2.4.min.js"></script>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <a class="navbar-brand" href="#!">Tidy Up</a>

      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-md-0">
          <% if user_signed_in? %>
            <li class="nav-item">
              <a class="nav-link" href="#!"><%= link_to "モノ一覧", items_path %><span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#!"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></a>
            </li>
          <% else %>
            <li class="nav-item">
              <a class="nav-link" href="#!"><%= link_to "新規登録", new_user_registration_path %></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#!"><%= link_to "ログイン", new_user_session_path %></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#!"><%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post %></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#!"><%= link_to "管理者ゲストログイン", users_admin_guest_sign_in_path, method: :post %></a>
            </li>
          <% end %>
        </ul>
      </div>
    </nav>

<!--
    <% if user_signed_in? %>
      <%= link_to "モノ一覧", items_path %>
      <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "新規登録", new_user_registration_path %>
      <%= link_to "ログイン", new_user_session_path %>
      <%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post %>
      <%= link_to "管理者ゲストログイン", users_admin_guest_sign_in_path, method: :post %>
    <% end %>
    --->

    <% if notice %>
      <p class="alert alert-notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-error"><%= alert %></p>
    <% end %>
    <%= yield %>

    <nav class="navbar navbar-light bg-light">
      <a class="navbar-brand" href="#!">©a-myzk</a>
    </nav>
  </body>
</html>
