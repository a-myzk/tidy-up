  <h1 class="field-content d-flex justify-content-center"><%= @user.name %>でログイン中。。。</h1>
  <div class="field-content d-flex justify-content-center">メールアドレス: <%= @user.email %></div>
  <div class="field-content d-flex justify-content-center "><%= link_to "モノ一覧画面にもどる", items_path, class: "btn btn-outline-dark" %></div>
